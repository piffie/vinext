{
  "app-dir": {
    "cache-components-errors": { "*": ["*"] },
    "app": {
      "index.test.ts": ["$contains:window.next.__internal_src_page"]
    },
    "app-static": {
      "app-static.test.ts": [
        "should honor fetch cache in generateStaticParams",
        "should not cache request if response data size is greater than 2MB and FetchCache is possible in development mode",

        "should bailout to client rendering - with suspense boundary",
        "$mode_start:should load data only at build time even if response data size is greater than 2MB and FetchCache is possible",

        "$mode_start:should not encode dynamic parameters as search parameters in RSC data",
        "$mode_start:should output HTML/RSC files for static paths",
        "$mode_start:should have correct prerender-manifest entries",

        "$mode_start:should stream properly for '/stale-cache-serving-edge/app-page'"
      ],
      "*": ["*"]
    },
    "*": ["*"]
  },
  "*": ["*"]
}
